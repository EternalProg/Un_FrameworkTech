services:
  app:
    build:
      context: .
      target: dev
    environment:
      NODE_ENV: development
      CORS_ORIGIN: '*'
    command: ['sh', '-c', 'npm run db:migrate && npm run seed && npm run dev']
    volumes:
      - .:/app
      - /app/node_modules
